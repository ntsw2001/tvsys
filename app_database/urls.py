from django.urls import path, include
from .views import *

urlpatterns = [
   #path('', Login.as_view(), name = "login"),
   #path('reset', views.edit_pass_init),
   #path('edit_password', views.edit_pass_normal),
]